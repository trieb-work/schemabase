overwrite: true
schema: "./libs/adapters/saleor/src/lib/schema.gql"
documents: "./libs/adapters/saleor/src/lib/{mutations,queries}/**/*.gql"
generates:
  ./libs/adapters/saleor/src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-generic-sdk
hooks:
  afterOneFileWrite:
    - prettier --write
