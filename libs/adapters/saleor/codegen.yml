overwrite: true
schema: "https://pundf-test-api.triebwork.com/graphql/"
documents: "./src/lib/{mutations,queries}/**/*.gql"
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-generic-sdk
hooks:
  afterOneFileWrite:
    - prettier --write
