import { integrationsStrapiOrdersToZoho } from "./integrations-strapi-orders-to-zoho";

describe("integrationsStrapiOrdersToZoho", () => {
  it("should work", () => {
    expect(integrationsStrapiOrdersToZoho()).toEqual(
      "integrations-strapi-orders-to-zoho",
    );
  });
});
