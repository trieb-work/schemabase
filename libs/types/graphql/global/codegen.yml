overwrite: true
schema: "https://pundf-test-api.triebwork.com/graphql/"
documents: "./src/lib/*"
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
hooks:
  afterOneFileWrite:
    - prettier --write
