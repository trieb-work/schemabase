mutation bulkOrderCreate($orders: [OrderBulkCreateInput!]!, $errorPolicy: ErrorPolicyEnum!) {
  orderBulkCreate(
    errorPolicy: $errorPolicy
    orders: $orders
    stockUpdatePolicy: SKIP
  ) {
    errors {
      code
      message
      path
    }
    results {
      order {
        errors {
          code
          field
          message
        }
        id
      }
    }
  }
}