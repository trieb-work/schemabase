mutation productTypeUpdate($input: ProductTypeInput!, $id: ID!) {
  productTypeUpdate(input: $input, id: $id) {
    productType {
      id
    }
    errors {
      field
      message
      code
    }
  }
}
