mutation createTaxClass($input: TaxClassCreateInput!) {
    taxClassCreate(input: $input) {
        errors {
            field
            message
            code
        }
        taxClass {
            id
        }
    }
}
