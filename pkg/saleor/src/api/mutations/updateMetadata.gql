 mutation saleorUpdateMetadata($id: ID!, $input: [MetadataInput!]!, $keysToDelete: [String!]!) {
  updateMetadata(id: $id, input: $input) {
    errors {
      field
      message
    }
  }
  deleteMetadata(id: $id, keys: $keysToDelete) {
    errors {
      field
      message
    }
  }
}