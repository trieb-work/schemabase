
image:
  imageName: ghcr.io/trieb.work/anwr-qualibet-statistics/bullboard

ingress:
  enabled: true
  host:
  tls:
    secretName:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod  
redis:
  password:
  host: ""
  service:
  port: 6379 
nodeEnv: production

google:
  clientId:
  clientSecret:

allowedLoginDomains: ["trieb.work"]
imagePullSecret: github
